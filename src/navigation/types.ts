export type RootStackParamList = {
  SessionList: undefined;
  NewSession: undefined;
  Analysis: { sessionId: string };
  Settings: { sessionId: string };
  Charts: { sessionId: string };
  Gantt: { sessionId: string };
  Yamazumi: { sessionId: string };
  Improvements: { sessionId: string };
};
